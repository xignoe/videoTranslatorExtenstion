{
  "name": "video-translator-extension",
  "version": "2.0.0",
  "description": "Chrome extension: live translated subtitles for any video, using on-device Whisper speech recognition",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.7.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
